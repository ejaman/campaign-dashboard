# Copy this file to .env.local and fill in the values
# cp .env.example .env.local

# API 서버 URL (json-server)
# 로컬: http://localhost:3001
# Vercel 배포 시 외부 json-server URL로 교체
NEXT_PUBLIC_API_URL=http://localhost:3001
